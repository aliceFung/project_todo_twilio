<tr>
<td><%= todo.id %></td>
<td><%= todo.text %></td>
<td><%= todo.completion_date %></td>
<td><%= todo.created_at %></td>
<td><%=link_to "Text yourself", send_path(todo.text)%></td>
<td><%= link_to "Edit", edit_todo_path(todo.id) %></td>
<td><%= link_to "Delete!", todo_path(todo.id), method: :delete %></td>
<tr>
